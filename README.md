
  # Portfolio Template with Animations

  This is a code bundle for Portfolio Template with Animations. The original project is available at https://www.figma.com/design/qiFfKIChvCKdlaphrFRQV0/Portfolio-Template-with-Animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  